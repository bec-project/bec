"""Package to handle "macros" in BEC lib. Macros are small functions intended for interactive use
to speed up often-repeated tasks. They are loaded into a namespace under the BECClient, in a
structure defined in a config file in a plugin repo. They can optionally be added to the global
built-ins namespace in that config file."""
